# Core data
pydantic>=2.7.0
pandas>=2.2.0
pyarrow>=16.0.0
numpy>=1.26.0

# Progress & logging
tqdm>=4.66.0
loguru>=0.7.2

# Testing
pytest>=8.2.0
pytest-cov>=5.0.0

# Retrieval — CPU-only, no network calls at runtime
rank-bm25>=0.2.2
sentence-transformers>=3.0.0

# Ranking — CPU-only (n_jobs=1, num_threads=1 enforced in code)
lightgbm>=4.0.0
